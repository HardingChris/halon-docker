version: "6.10"
listeners:
  - port:  {{ .Values.global.smtpd.api.service.port }}
environment:
  privdrop:
    user: api
    group: api
  controlsockets:
    smtpd:
      port: {{ .Values.controlSocketPort }}
